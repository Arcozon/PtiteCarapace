~ exp at exec
* at exec

ReadlineChild : double ctrlC FIXED
			free T_ms
			Init status dans get_cmd_line_fd

T_ms: ajouter fd_history, alias, prompt

env init, set PWD OLDPWD SHLVL

alias error msg

export PATH=    -> error execve [fixed, can be better]

check redir order

Syntaxe : pas un logop('\0' | ; && ||) avant une par -> error
 expand ~ en meme temps que $, seulement en dehors des quotes

Custom prompt pour les quotes, pipes lines, opp, par open

newline ctrl C\ pendant l exec
-- ^\Quit (core dumped)

cat ; cat ->ctrl C fcked
