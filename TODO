~ exp at exec
* at exec

ReadlineChild : double ctrlC FIXED
			free T_ms
			Init status dans get_cmd_line_fd

T_ms: ajouter fd_history, alias, prompt

env init, set PWD oLD PWD SHLVL PROMPT_FORMAT

pb sur la detection d alias ??

alias use fds[2]

check redir order

enlever les getenv

export PATH=    -> error execve [fixed, can be better]

status when no cmd after ;
