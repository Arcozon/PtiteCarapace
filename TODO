~ exp at exec
* at exec

Syntax: ';' can have nothing after

ReadlineChild : double ctrlC FIXED
			free T_ms
			Init status dans get_cmd_line_fd

T_ms: ajouter fd_history, alias, prompt

prompt : prompt vert et prompt rouge ?
			maj du pwd dans le prompt

env init, set PWD oLD PWD SHLVL

Error when cmd is not found

mettre le minishellrc dans le repertoire courrant

pb sur la detection d alias

SRC	+=	prompt/generate_prompt.c\
		prompt/write_prompt.c

// add \p \n dans le prompt

r_value qui reset quand une input est vide
